<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Events | Web Portal</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <header class="header">
    <div class="container">
      <h1 class="logo">Web Portal</h1>
      <nav class="nav">
        <a href="/" class="nav-btn">Home</a>
        <a href="/about" class="nav-btn">About</a>
        <a href="/events" class="nav-btn">Events</a>
        <a href="/contact" class="nav-btn">Contact</a>
      </nav>
    </div>
  </header>



  <main class="main-content container">

    <section class="welcome">
      <h2>Welcome to Our Events Page!</h2>
      <p>Stay tuned for exciting upcoming events and updates. We're glad you're here.</p>
    </section>

    <section class="events-placeholder">
      <h3>Upcoming Events</h3>
      <% if (events && events.length > 0) { %>
        <div class="event-list">
          <% events.forEach(event => { %>
            <div class="event-card" style="margin-bottom: 2rem;">
              <img src="/images/<%= event.image %>" alt="<%= event.title %>" style="width:100%; border-radius: 8px; margin-bottom: 0.8rem;" />
              <h4 style="color: #f5f5f5;"><%= event.title %></h4>
              <p><strong>Date:</strong> <%= event.date %></p>
              <p><strong>Location:</strong> <%= event.location %></p>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <p class="placeholder">No events have been scheduled yet.</p>
      <% } %>
    </section>
  </main>

  <footer class="footer">
    <p>&copy; 2025 Web Portal. All rights reserved.</p>
  </footer>
</body>
</html>
